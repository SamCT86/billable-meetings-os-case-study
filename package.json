{
  "name": "billable-meetings-reference",
  "version": "1.0.0",
  "private": true,
  "description": "Public executable reference for deterministic meeting-billing truth boundaries.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "verify": "node --test"
  },
  "engines": { "node": ">=20" }
}
